# nvinfer config for YOLOv11s Jockey Detector — 1 camera debug mode
#
# batch-size=1, interval=0 (YOLO on every frame)

[property]
gpu-id=0

# Model paths — will auto-rebuild engine for batch=1 from ONNX
onnx-file=/home/user/race_vision/models/jockey_yolov11s.onnx
model-engine-file=/home/user/race_vision/models/jockey_yolov11s.onnx_b25_gpu0_fp16.engine

# Network parameters
batch-size=25
# 0=FP32, 1=INT8, 2=FP16
network-mode=2
network-input-order=0

# interval=0 → YOLO on EVERY frame (no skipping)
interval=0

gpu-id=0
output-tensor-meta=0

# Input dimensions (must match ONNX export: 800x800)
infer-dims=3;800;800

# 1 class: jockey
num-detected-classes=1
process-mode=1

# Preprocessing (same as standard YOLOv8/v11)
maintain-aspect-ratio=1
symmetric-padding=1
net-scale-factor=0.00392157
model-color-format=1

# NMS
cluster-mode=2

# Custom YOLOv8 parser
parse-bbox-func-name=NvDsInferParseYoloV8
custom-lib-path=/home/user/race_vision/deepstream/build/libnvdsinfer_yolov8_parser.so

output-blob-names=output0

[class-attrs-all]
pre-cluster-threshold=0.15

[class-attrs-0]
pre-cluster-threshold=0.15
